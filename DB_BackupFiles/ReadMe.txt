You can ignore the tables starting with an x they aren't used in the api.  They are either backup tables or transitionary tables to stage the data before it was checked and inserted to the live tables.  

Is used the Database name movie_db and was running a MySql container in Docker.